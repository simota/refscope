# Nexus Journal

| Date | Task | Chain | Mode | Decision |
|---|---|---|---|---|
| 2026-04-30 | グラフィカルな要素を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | 単一 UI 改善のためサブエージェントは使わず、timeline に commit activity overview を追加 |
| 2026-04-30 | sidebar に ref map を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | 既存 refs data だけで branches/remotes/tags の分布と選択 ref を SVG で可視化 |
| 2026-04-30 | detail panel に change graph を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | 既存 changed-file stats だけで additions/deletions の比率とファイル別変更量を可視化 |
| 2026-04-30 | compare mode に graph を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | compare result の ahead/behind と added/deleted をラベル付きバーで可視化 |
| 2026-04-30 | top bar に live pulse を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | realtime status と paused pending updates を小さな pulse bar で可視化 |
| 2026-04-30 | rewrite alert に flow visual を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | previous hash から current hash への rewrite flow を SVG とラベルで可視化 |
| 2026-04-30 | activity overview に author graph を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | 表示中 commits の author distribution をラベル付きバーで可視化 |
| 2026-04-30 | detail panel に file status mix を追加 | Nexus internal -> frontend patch | AUTORUN_FULL | changed files の status 構成を分割バーと凡例で可視化 |
| 2026-05-01 | Plea Hana demand に対する PROPOSE 提案 | Nexus -> Spark | AUTORUN_FULL | minimum viable chain (Spark 単独 spawn, opus) で「期間サマリビュー」3 options を生成。観察 / 派生境界を rewrite alert 語彙で再利用する Option B を推奨。Next: Rank |
| 2026-05-01 | Plea round 2 demand 4 件の priority scoring | Nexus -> Rank | AUTORUN_FULL | minimum viable chain (Rank 単独 spawn, opus) で RICE / WSJF / MoSCoW / ICE 横断スコア。推奨順序 Yuki → Hana → Ken (Spike 並走) → Tomo (v2)。Ken の reflog allowlist 追加は Architecture review が必要と flag。Next: Researcher (Ken 検証) + Sherpa (Yuki/Hana decomposition) の並走 |
| 2026-05-01 | D-Yuki Quiet mode 実装 (Rank 1 位の先行実装) | Nexus -> Artisan | AUTORUN_FULL | minimum viable chain (Artisan 単独 spawn, opus)。`useQuietMode` hook を新規追加し、`data-quiet` root attribute + CSS で全体抑制、`livePaused` (manual) と `isQuiet` (auto) を独立保持して effective pause を合成。`prefers-reduced-motion` 同期、localStorage 永続化、polite live region で missed event 告知。`pnpm build:mock` ✓ / API tests 58/58 ✓。UI のブラウザ動作確認は未実施 — Next: dev server smoke + Echo cognitive walkthrough |
| 2026-05-01 | D-Tomo file-level history proposal (v2 候補 RFC) | Nexus -> Spark | AUTORUN_FULL | Spark 単独 spawn (opus) で 865 行 RFC を生成。Option B 推奨 (新 endpoint `/files/history`、opaque base64 cursor、Git literal `R<NN>` を payload にそのまま転記)。200 commit hard limit 維持、gitRunner allowlist 変更不要。Next: Researcher (大規模 maintainer 検証) → GO 後 Sherpa decomposition |
| 2026-05-01 | D-Hana 期間サマリビュー API 実装 (Rank 2 位) | Nexus -> Builder | AUTORUN_FULL | Builder 単独 spawn (opus) で `GET /api/repos/:repoId/commits/summary` を Spark Option B contract で実装。validation.js に `parseDateQuery` / `parseGroupByQuery`、gitService.js に `summarizeCommits`、http.js にルート追加。`--no-show-signature --no-renames --max-count=201 --since --until --end-of-options` 経由、`truncated: true` で 200 cap 透明化。tests 58 → 85 件 (+27)。gitRunner allowlist 変更なし。Next: Artisan (UI) |
| 2026-05-01 | D-Hana 期間サマリビュー UI 実装 | Nexus -> Artisan | AUTORUN_FULL | Artisan 単独 spawn (opus) で `PeriodSummaryView.tsx` を新規追加 (19 KB)、Observed (no interpretation) と Derived (rule-based grouping, no AI) を別カードで分離 (rewrite alert と同じ warning border 再利用)、period preset (today/this-week/last-30/custom) と groupBy tabs (prefix/path/author)、truncated バッジ、uncategorized は "kept separate, not guessed" 文言で表示。drilldown は既存 timeline filter (search/path/author) に流す最小実装。`pnpm build:mock` ✓ / API tests 85/85 ✓。ブラウザ目視は未実施 — Next: dev server smoke + Echo |
| 2026-05-01 | diff viewer 本格改修 (旧 viewer は 行番号偽装、file 構造なし) | Nexus -> Artisan | AUTORUN_FULL | Artisan 単独 spawn (opus)。新規 `parseUnifiedDiff.ts` (434 行、`ParsedDiff`/`DiffFile`/`DiffHunk`/`DiffLine` AST、changeKind 8 種、similarity / isBinary / 行番号を hunk header から正確に算出、throw しない)、新規 `DiffViewer.tsx` (913 行、file list + viewMode all/single + wordWrap + showWhitespace + filter + collapse + 個別コピー + truncated 警告)。API 変更ゼロ、新規依存ゼロ、新 CSS variable ゼロ。aria-pressed/expanded/selected/label/live 完備、quiet mode 互換 (data-quiet CSS)。`pnpm build:mock` ✓ (+30 KB JS) / API tests 85/85 ✓。Risks: parser 専用 unit test 不在、500+ file 全展開の DOM 爆発リスク、HTTP プロキシ越し clipboard 失敗。Next: Radar (parser unit tests) |
| 2026-05-01 | DiffViewer に fullscreen モード追加 | Nexus -> Artisan | AUTORUN_FULL | Artisan 単独 spawn (opus)、4 ファイル編集 (DiffViewer.tsx / DetailPanel.tsx / CommandPalette.tsx / App.tsx)。Controlled/uncontrolled hybrid (`fullscreenProp` + `onFullscreenChange`)、CSS overlay (`role="dialog" aria-modal="true"` + `position: fixed; inset: 0; z-index: 50`)、body scroll lock + cleanup、Escape 復帰、focus return (前 toggle へ戻す)、commit 切替で auto close、CommandPalette に "Toggle diff fullscreen" 追加 (diff 利用可能時のみ)。新規依存ゼロ、新 CSS variable ゼロ、API 変更ゼロ。`pnpm build:mock` ✓ (+3 KB JS) / API tests 85/85 ✓ |
| 2026-05-01 | 3 ペインを resizable に + sidebar 折りたたみ + デフォルト比率調整 (Diff 領域 ~25% → ~40%) | Nexus -> Artisan | AUTORUN_FULL | Artisan 単独 spawn (opus)、6 ファイル編集 + `useLayoutPrefs.ts` 新規。shadcn `Resizable` primitive (`react-resizable-panels` wrap) で 3 ペイン (sidebar 18 / center 42 / detail 40)、min/max 制約、collapsible sidebar via `ImperativePanelHandle.collapse/expand()`、`onLayout` rAF coalesce で localStorage `refscope.layoutPrefs.v1` 永続化 (schema validation + fallback)。TopBar に `PanelLeftClose/Open` トグル (aria-pressed)、CommandPalette に "Toggle branch sidebar"、固定幅 (BranchSidebar 248px / DetailPanel 460px) を撤廃。`pnpm build:mock` ✓ (+57 KB JS for resizable) / API tests 85/85 ✓ / `make verify` 8/8 ✓ |
