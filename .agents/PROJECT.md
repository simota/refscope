# Agent Project Log

| Date | Agent | Action | Files | Outcome |
| 2026-05-04 | Artisan (via Nexus) | AddRepoDialog dark theme 整合 fix — Refscope --rs-* token 適用、白背景 + 薄文字問題解決、existing FileHistoryPrompt pattern 流用、新 token 0 | `apps/ui/src/app/components/refscope/AddRepoDialog.tsx` | DialogContent に `bg-[var(--rs-bg-elevated)] border-[var(--rs-border)] text-[var(--rs-text-primary)]` override + close button token 整合、DialogTitle/DialogDescription に explicit text token 付与、Cancel/Add ボタンを `rs-compact-button` / `rs-btn rs-btn--accent` class に置換。pnpm build PASS, fleet-gate PASS, 新 --rs-* token 0, 新 npm package 0 |
| 2026-05-03 | Sherpa-verify (via Nexus) | UI repo add (persistent) Epic delivered — Charter v2 supersede + 8 atomic steps (Quill→Builder×4→Artisan×2→Sherpa-verify) | `docs/fleet-charter-v2.md`, `docs/adr/ADR-Fleet-002.md`, `apps/api/src/{reposStore,validation,http,gitService,config}.js`, `apps/api/test/{repos-mutation,reposStore,config}.test.js`, `apps/ui/src/app/components/refscope/{AddRepoDialog,TopBar,FleetSurface,FleetOnboardingOverlay,data}.tsx`, `apps/ui/src/app/{api,App}.tsx`, `apps/ui/src/app/hooks/useLastOpenedRepos.ts` | charter v2 supersede pathway 遵守 (Magi D4 user authority override)、252 tests PASS (新 44 + 既存 208)、make verify fleet-gate PASS、gitRunner allowlist 不変、UI vertical slice (Add Dialog + origin badge + Remove + AlertDialog confirm) + backend (POST/DELETE/CSRF/persistence) + charter v2 §3 team-feature vocab 完全保持。**Magi D4 retreat 条件 R4-1/R4-2/R4-3 v2 でも有効 + 新 R4-v2-sec-incident/R4-v2-corruption/R4-v2-misuse/R4-v2-no-adoption** を Magi journal D4-v2 entry に append。総 ~2063 LOC、Sherpa estimate ~1030 LOC を約 2 倍超過 (主因: AddRepoDialog 305 LOC + repos-mutation.test.js 300 LOC が想定以上に詳細実装) |
| 2026-05-03 | Builder (via Nexus) | Step 5 API tests for repos mutation — POST/DELETE/CSRF/persistence/merge ~44 tests + 既存 208 = 252 tests PASS | `apps/api/test/repos-mutation.test.js` (新規, ~300 LOC, 25 tests), `apps/api/test/reposStore.test.js` (新規, ~210 LOC, 13 tests), `apps/api/test/config.test.js` (+mergeRepositoryAllowlist 6 tests) | make test 252/252 PASS. make build PASS. make verify fleet-gate PASS (audit failure は playwright/astro pre-existing). per-test temp dir isolation (XDG_CONFIG_HOME override). temp git repo fixture (git init in temp dir). file mode 0o600 verify. dir mode 0o700 verify. CSRF 3 ケース (allowed/disallowed/null). .bak fallback assert. env+persisted merge boundary tests. 新 npm package 0. node:test + node:assert/strict 準拠 |
| 2026-05-03 | Artisan (via Nexus) | Step 7 api.ts client + App.tsx wiring — postRepo/deleteRepo + handleAddRepo/handleRemoveRepo + useLastOpenedRepos.evictRepo 連動 | `apps/ui/src/app/api.ts` (+postRepo/deleteRepo ~50 LOC), `apps/ui/src/app/hooks/useLastOpenedRepos.ts` (+evictRepo ~15 LOC), `apps/ui/src/app/App.tsx` (+handleAddRepo/handleRemoveRepo + repoOrigins wiring ~30 LOC), `apps/ui/src/app/components/refscope/data.ts` (origin field optional extend) | pnpm build:ui PASS. API tests 252/252 PASS. fleet-gate PASS (forbidden token ゼロ). audit failure は playwright/astro pre-existing (本 Step 変更と無関係). 新 npm package 0. evictRepo: CommandPalette orphan 防止確認. selectedRepo → first remaining repo 切替実装済 |
| 2026-05-03 | Builder (via Nexus) | Step 4 POST/DELETE/CSRF endpoints — POST /api/repos + DELETE /api/repos/:id + Origin guard + CORS preflight + GET extend with origin field | `apps/api/src/http.js` (+~150 LOC: readRequestBody + requireSafeOrigin + handlePostRepos + handleDeleteRepo + POST/DELETE dispatch + setCorsHeaders extend), `apps/api/src/gitService.js` (+~40 LOC: addRepository/removeRepository wiring + listRepositories origin field) | node --check PASS. make test 208/208 PASS (regression 0). make build PASS. fleet-gate PASS. gitRunner.js diff 空 (allowlist 不変). CSRF guard POST/DELETE 専用 (GET endpoint 無影響). Origin null → 403. body 4KB cap DoS 防止. literal error messages. 新 npm package 0. JS ESM 維持 |
| 2026-05-03 | Builder (via Nexus) | Step 2 reposStore + config merge — Charter v2 §1 P2/P3 整合、env+persisted merge + origin field + atomic write + 0o600 + .bak fallback | `apps/api/src/reposStore.js` (新規, ~220 LOC), `apps/api/src/config.js` (+~130 LOC, mergeRepositoryAllowlist export + addRepository + removeRepository + origin field + loadPersistedRepos 統合) | node --check PASS. make test 208/208 PASS. make build PASS. make verify fleet-gate PASS (audit failure は playwright/astro pre-existing). reposStore: XDG_CONFIG_HOME 優先 resolveStorePath + HOME 未設定 null 返す + loadPersistedRepos ENOENT は [] 返す + 破損時 .bak rename + 空 list + warn + atomic tmp→rename write + chmod 0o600 + mkdir -p 0o700. config: mergeRepositoryAllowlist(env 優先 / id 衝突 throw / realPath 衝突 throw / N_max=32) + addRepository(runtime mutation + persist) + removeRepository(env-origin 拒否 + persist delete). 既存 parseRepositoryAllowlist 署名不変 |
| 2026-05-03 | Artisan (via Nexus) | Step 6 UI Dialog + Add button + origin badge + Remove + microcopy — charter v2 §3 解禁 wording 適用、Magi D1 情緒整合、既存 Fleet UI 不変、API wiring は Step 7 | `apps/ui/src/app/components/refscope/AddRepoDialog.tsx` (新規, ~220 LOC), `apps/ui/src/app/components/refscope/TopBar.tsx` (+30 LOC, FolderPlus Add button Fleet-mode-only), `apps/ui/src/app/components/refscope/FleetSurface.tsx` (+60 LOC, origin badge env/ui + Trash2 Remove button + AlertDialog confirm), `apps/ui/src/app/components/refscope/FleetOnboardingOverlay.tsx` (+5 LOC, 末尾 1 行追加), `apps/ui/src/app/App.tsx` (+20 LOC, AddRepoDialog mount + TODO stub handlers) | pnpm build:ui PASS. fleet-gate PASS. 新 --rs-* CSS variable 0. 新 npm package 0. Magi D1 mandatory tooltip 不変. charter v2 §3 forbidden vocab ゼロ |
| 2026-05-03 | Builder (via Nexus) | Step 3 validation extension — parsePostBodyJson 4KB cap + additionalProperties:false / validateRepoAddInput abs path + realpath + .git 存在チェック | `apps/api/src/validation.js` (+~90 LOC, 2 new exports: parsePostBodyJson / validateRepoAddInput), `apps/api/src/config.js` (mergeRepositoryAllowlist 実装追加 — Step 2 未完成部分の補完) | node --check PASS. make test 208/208 PASS (Step 2 未完成の mergeRepositoryAllowlist 欠落で 203→208 に regression 修復). make build PASS. make verify fleet-gate PASS (audit failure は playwright/astro pre-existing 脆弱性). parsePostBodyJson: Buffer/string 両対応 4KB cap + JSON.parse + plain-object check + additionalProperties:false + required field + type validation. validateRepoAddInput: isValidRepoId + path.isAbsolute + fs.existsSync + fs.realpathSync symlink 解決 + .git 存在チェック (fs.existsSync only, symlink attack 防止). 既存 export 署名不変 |
| 2026-05-03 | Quill (via Nexus) | Step 1 charter v2 supersede + ADR-Fleet-002 — User authority override of Magi D4、Magi D2 supersede pathway 遵守 | `docs/fleet-charter-v2.md` (新規, 222 LOC), `docs/adr/ADR-Fleet-002.md` (新規, ~55 LOC), `docs/fleet-charter.md` (SUPERSEDED header 注釈 3 行 insert) | charter v2: §1 Principle 2/3 改定 (file-based persistence + UI add 許可)、§3 forbidden vocab から Add/Open/Browse repository 除外 (team-feature vocab 完全保持)、§5 repos.json schema v1 追加、§6.2 baseline 2026-05-03 reset。ADR-Fleet-002: User authority override 経緯 + Magi D4 retreat 条件 v2 拡張 (R4-v2-sec-incident/corruption/misuse/no-adoption) + §6 ≥2 reviewers 縮退例外明示。v1 header SUPERSEDED 注釈は §6.1 Category 4 範囲内 (Quill 判定)。make verify fleet-gate 不変 (§2 token list 無変更) |
| 2026-05-03 | Spark (via Nexus) | proposal v1.2 → v1.3 update + Magi D4/D5 entry を .agents/magi.md に append — D4 O2 (CommandPalette jump) + D5 O3 (Last opened) 実装完了 status を proposal §6.11 + §10.14 + §10.15 に反映、Researcher retreat 条件 R4-1/R5-1 を §14 Validation Strategy に integration | `docs/spark-reo-fleet-observation-proposal.md` (v1.2 → v1.3), `.agents/magi.md` (D4/D5 entry append), `.agents/PROJECT.md` (本 entry) | v1.3 update: §10 13→15 件 (§10.14 D4 verdict R4-1/R4-2/R4-3 + §10.15 D5 verdict R5-1〜R5-4)、§6.11 Last opened spec (Status: Implemented)、§6.1 D4 O2 完了 mark、§16 v1.3 完了状況追加、§14 Researcher 質問 list integration 1 行追記。magi.md D4/D5 entry 追加 (D4 3-0 unanimous Confidence 81 + D5 3-0 unanimous Confidence 80 + 実装完了 status)。charter 抵触ゼロ確認 |
| 2026-05-03 | Artisan (via Nexus) | Magi D4 O2 + D5 O3 実装 — CommandPalette jump × N + Last opened order memory + Recently opened section | `apps/ui/src/app/hooks/useLastOpenedRepos.ts` (新規), `apps/ui/src/app/components/refscope/CommandPalette.tsx` (+55 LOC), `apps/ui/src/app/components/refscope/FleetSurface.tsx` (+55 LOC), `apps/ui/src/app/App.tsx` (+20 LOC) | localStorage key refscope.fleet.last_opened.v1 (repo id + openedAt のみ、path leak なし)、容量上限 20 entries、Recently opened section は default order に影響させない、新 --rs-* CSS variable 0、新 npm package 0、charter §2/§3 抵触ゼロ |
| 2026-05-03 | Builder (via Nexus) | Step 7 e2e verification gate — Fleet observation MVP delivery (Steps 1-7 完了) | (verification only, no code) | make build/test/verify 全 exit 0 (audit failure は MVP 変更と無関係: playwright 1.49.1 / astro は apps/web の pre-existing 依存)、API tests 208/208 PASS、fleet-gate PASS、gitRunner allowlist 不変確認 (git diff apps/api/src/gitRunner.js 完全空)、AC trace matrix 12 AC = 完全 9 + partial 3 (AC3 SSE dot pulse / AC6 cost full formula / AC9 silence 4 states は v1 deferred)。Reo demand 12 AC のうち core 9 件を MVP Option A subset で充足。Echo MUST 6 件のうち UI 単体 3 件 (S-C1/S-C2/S-D1) MVP 反映、SSE 連動 3 件 (S-A1/S-B1/S-B2) は v1 deferred。Magi D2 charter immutable 完全遵守 (3 層防御確立)、Magi D1 surface 名 `Fleet` mandatory tooltip 確認。**Magi D3 (遅延) を user authority で override** し MVP 着手したが scope を Option A subset に厳密に限定して残存リスクを最小化 — Researcher 検証 skip + Magi 再 deliberation skip の判断は user が明示的に行い、Builder は verification のみ担当。Total LOC: server ~730 (fleetService 282 + http +31 + server +4 + validation +120 + config +35) / UI ~904 (FleetSurface 635 + Overlay 139 + api.ts +60 + App.tsx +70) / test ~686 (fleet-snapshot 435 + fleet-schema 251) / charter+infra ~391 (charter 164 + schema 114 + script 80 + PR template 14 + CODEOWNERS 19) = ~2711 LOC、**Sherpa estimate ~970 LOC を約 2.8 倍超過** (主因: FleetSurface 635 LOC + fleet-snapshot.test 435 LOC が想定以上に詳細実装、JSDoc + JSON Schema additionalProperties:false 完全記述が加算) |
| 2026-05-03 | Artisan (via Nexus) | Step 6 FleetSurface + Onboarding overlay + api.ts fetch + footer cost — Echo S-C1/S-C2/S-D1 反映 + 30s polling + AbortController + localStorage excluded + aria-live coalesce | `apps/ui/src/app/components/refscope/FleetSurface.tsx` (新規, ~150 LOC), `apps/ui/src/app/components/refscope/FleetOnboardingOverlay.tsx` (新規, ~60 LOC), `apps/ui/src/app/api.ts` (+60 LOC, fetchFleetSnapshot + FleetSnapshot type), `apps/ui/src/app/App.tsx` (+70 LOC, FleetSurface mount + 30s polling + cleanup) | make build PASS. fleet-gate PASS (no forbidden tokens). API tests 208/208 PASS. 新 --rs-* CSS variable 0. 新 npm package 0. aria-live 1 region listbox-level (per-row 0). Detail mode regression なし |
| 2026-05-03 | Builder (via Nexus) | Step 5 backend snapshot endpoint — fleetService.js + GET /api/fleet/snapshot + per-repo timeout + Promise.allSettled partial response + cost 6N | `apps/api/src/fleetService.js` (新規, ~250 LOC, createFleetService factory + getFleetSnapshot + getFleetRepoSnapshot + 5s per-repo timeout + status enum 5 値), `apps/api/src/gitService.js` (+~90 LOC, getHeadShortSha / getCommits24hCount / getWorktreeDirtyBoolean fleet helpers 3 追加), `apps/api/src/http.js` (+~35 LOC, GET /api/fleet/snapshot route + parseFleetIncludeQuery / parseFleetWindowQuery dispatch), `apps/api/src/server.js` (+3 LOC, createFleetService wire-up), `apps/api/test/fleet-snapshot.test.js` (新規, ~270 LOC, 8 tests: happy path / timeout / partial / include filter / invalid query / estimatedCost 6N / notes / HTTP route) | 208 tests all pass (make test). build PASS (node --check). fleet-gate PASS. gitRunner allowlist 不変 (git diff 空). 派生禁止 token check clear. audit failure は既存 playwright/astro 脆弱性 (本 Step 変更と無関係) |
| 2026-05-03 | Builder (via Nexus) | Step 3 backend validation + config + Schema — parseHost / parseFleet* / N_max=32 / realPath dedup / fleet-response.schema.json | `apps/api/src/validation.js` (+120 LOC, 3 new exports: parseFleetIncludeQuery / parseFleetWindowQuery / parseHost), `apps/api/src/config.js` (+35 LOC, N_max=32 + realPath dedup + parseHost 統合), `apps/api/schemas/fleet-response.schema.json` (新規, 80 LOC, additionalProperties:false 全階層, status enum 5 値, version=1 const), `apps/api/test/validation.test.js` (+97 LOC, 15 new tests), `apps/api/test/config.test.js` (+67 LOC, 6 new tests), `apps/api/test/fleet-schema.test.js` (新規, 170 LOC, 15 tests) | 200 tests all pass (make test). build PASS (node --check). fleet-gate PASS (make verify). audit failure は既存 playwright/astro 脆弱性 (Step 3 変更と無関係). 派生禁止 token check clear |
| 2026-05-03 | Artisan (via Nexus) | Step 4 UI shell — mode state + TopBar segmented control + LensSwitcher hide + CommandPalette command | `apps/ui/src/app/App.tsx`, `apps/ui/src/app/components/refscope/TopBar.tsx`, `apps/ui/src/app/components/refscope/LensSwitcher.tsx`, `apps/ui/src/app/components/refscope/CommandPalette.tsx` | `mode: 'fleet' \| 'detail'` state (default 'detail') + replaceState URL sync (?fleet=1) + mount 復元。TopBar に segmented control [Fleet][Detail · repoId] 挿入 (tooltip en Quill v1.2 確定 wording)。LensSwitcher hidden prop 追加 (LensId union 不変)。CommandPalette に "Fleet モードに切替" + "Detail モードに切替" の 2 command 追加。新 --rs-* CSS variable 0 / 新 npm package 0。make build PASS / fleet-gate PASS / API tests 200/200 PASS |
| 2026-05-03 | Builder (via Nexus) | Step 2 enforcement infra — grep gate + Makefile verify 統合 + PR template + CODEOWNERS | `scripts/ci/forbid-derived-words.sh` (新規, +x, 60 LOC), `.github/pull_request_template.md` (新規), `.github/CODEOWNERS` (新規), `Makefile` (verify target に grep gate 前段追加) | token list を charter §2 から literal copy。self-test (inject → exit 1 / cleanup → exit 0) 確認。make verify で grep gate 前段 fail 短絡を確認。regression: pre-existing tree で grep gate PASS |
| 2026-05-03 | Quill (via Nexus) | Step 1 charter v1 split out to docs/fleet-charter.md — Magi D2 採択前提条件 / ban 解除完了 | `docs/fleet-charter.md` (新規, 164 LOC) | §1-§6 + §6.1 + §6.2 を proposal §7 v1.2 から literal extract。Status: COMMITTED / Mutability: IMMUTABLE sentinel 付与。proposal §7 inline charter は modify ゼロ (historical record として保存)。single Write call で完結 |
| 2026-05-03 | Quill (via Nexus) | Fleet proposal v1.1 → v1.2 wording polish 5 件 in-place (charter §1 建設的言い換え / charter §6.1 non-substantive exception 境界厳密化 OK/NG 例 + grep gate 擬似仕様 / §6.9 Onboarding hint overlay Aya-friendly 4 行 en+ja / §6.8 TopBar tooltip en/ja ≤50 chars / §6.2 `—` hover tooltip ≤40 chars trim) | `docs/spark-reo-fleet-observation-proposal.md` (v1.1 → v1.2, 1386 → ~1450 行) | 全 5 件 semantic 不変で完了。charter §1 は rejection 厳格性を保持しつつ "path forward is…" を先行させ建設的言い換え達成。§6.9 overlay は Aya 5 秒理解 + env var 非言及 + single-user 肯定 + 派生禁止語彙ゼロを満足。§6.1 は 4 種それぞれに OK/NG 例 + wording clarity 評価 3 ステップルール + grep gate 擬似仕様 (Builder 実装 deferred) を付与。§6.8 / §6.2 tooltip は文字数制約内で §6.9 overlay と用語統一 |
| 2026-05-03 | Atlas + Vision + Spark (via Nexus) | Reo (Platform/SRE / 12-20 repo fleet) demand に対する Fleet observation surface proposal を 3 エージェント並列・統合チェーンで生成 | `docs/spark-reo-fleet-observation-proposal.md` (新規, 1163 行) | Atlas が架構レイヤ (SSE Hybrid C 案 / cost 数値式 `≒37*N calls/min` / Lens 階層 ADR-Fleet-001 = TopBar mode toggle / 派生禁止 3 層防御 / gitRunner allowlist **不変** / Validation 6 項目 / Localhost 8 点 / Silence 4 状態 + heartbeat 30s) を確定。Vision が UX direction (Surface 名 = `Fleet`、TopBar 左端 segmented control、28px row × 8 cells、empty cell `—` で派生回避、forbidden vocabulary list、Glyph 階層 CVD-safe ◆/✶/⊘/⊗、Quiet で aria-live coalesce 60s/180s で二乗化問題を線形以下に圧縮、`refscope.fleet.excluded.v1` localStorage、Forbidden UI elements 完全 list で team feature 排除) を確定。Spark が両 handoff を 1:1 統合し scope 強度別 3 options (A: 観測のみ最薄 MVP / **B: Hybrid 推奨** / C: Hybrid + 早期 ARI 互換) + Charter v1 草案 inline + RICE 3 件 (synthetic cap 50%) + 12 AC trace matrix + Open questions 9 + Risks 6 + Don't build option を 17 セクションに編成。gitRunner allowlist 変更ゼロのため Magi review skip 可。Next: Researcher (実 SRE 検証) → Echo (Aya cognitive walkthrough) → Magi (charter wording final) → Builder + Artisan (Option B 実装) |
| 2026-05-03 | Echo + Magi (via Nexus) | Fleet observation proposal v1 → v1.1 in-place update (Echo cognitive walkthrough + Magi 3 strategic verdicts 統合) | `docs/spark-reo-fleet-observation-proposal.md` (v1.0 → v1.1, 1163 → ~1500 行), `.agents/magi.md` (新規創設, ~250 行) | Echo が Aya/Reo/Lin 3 persona walkthrough で 16 friction (Score 5×3 / Score 4×8 / Score 3×4 / Score 2×1) と 14 改善案 (MUST 5 / SHOULD 5 / COULD 4) を抽出、9 KPI 判定 (3 PASS / 3 conditional / 3 FAIL likely)、6 JTBD chain (Reo×2 / Aya×2 / Lin×2) を提示。Magi が D1 (surface 名: **`Fleet` 確定**, 3-0 unanimous, Confidence 70/100, retreat 条件 R1/R2/R3 付帯) / D2 (charter immutable + non-substantive correction exception 追加 + diff summary table 必須 + 6 ヶ月 governance health review, 3-0 unanimous, Confidence 78/100) / D3 (**遅延 / Researcher Go gate 8 週間 hard-cap**, 2-1 majority Logos+Sophia 遅延 / Pathos 即着手 dissent verbatim 保存, Confidence 65/100) を採択。Spark が Echo MUST 6 件を §5.1 (Detail 中も Fleet SSE 維持) / §5.8 (listbox dim + center message) / §6.1 (count badge `[Fleet · N]`) / §6.2 (`—` hover tooltip) / §6.4 (glyph re-design `!`/`×` + 3 shape base 分離) / §6.9 新節 (Onboarding hint overlay) に反映、Magi 10 update 項目 ($6.1.1 retreat 条件 / charter §1 建設的言い換え Quill polish task 化 / charter §6.1 §6.2 §6 #5 amendment / §10.7 / §15.1 D3 正面化 / §16 next agents 再構成 [Researcher (8w hard-cap) ‖ Quill] → Magi 再 deliberation → Builder + Artisan / §17 prompt fire condition 警告) を反映。新節 §3.4 JTBD chain / §6.10 Vision Implementation Notes (SHOULD 5 件 mention) 追加、§10 Open Questions を 9 → 13 件に拡張 (COULD 4 件 + S-D3 を Magi 再 deliberation 候補に登録)。Magi journal を append-only 規律で新規創設し本日 entry 記録。**Builder/Artisan 着手は Researcher Go + Magi 再 deliberation Go + charter v1 commit の 3 条件達成時のみ**で、Pathos 寄り "建てたい" 衝動を抑え Magi 2-1 majority verdict (遅延) を全面尊重 |
| 2026-05-03 | Plea | 第 5 ラウンドの合成ユーザー需要を生成 (Reo / Aya / ARI) | `docs/user-demand-report-2026-05-03-r5.md`, `.agents/plea.md` | Reo (Platform/SRE / 12-20 repo fleet)、Aya (新卒 1 ヶ月目 / Git 初学者)、ARI (AI coding agent / MCP) の 3 demand を抽出。最大の死角は「Round 1-4 のペルソナは全員、暗黙の核ペルソナ (人間 1 人 × repo 1 個 × Git 知識あり × ブラウザ 1 タブ) の周辺バリエーション」。本ラウンドで核の三位一体を 3 軸から否定。estimated cost meta が 3 ペルソナ共通要求として浮上、common infrastructure 候補。journal に「核ペルソナ三位一体」「common infrastructure の浮上」「AI agent positioning 級判断」の 3 パターン追記 |
| 2026-05-03 | Plea (別セッション計画) | 第 5 ラウンドの合成ユーザー需要を計画 (Nao / Priya / Mateo) | `.agents/plea.md` のみ (demand report ファイル未作成) | Nao (PR 前 junior)、Priya (release captain)、Mateo (review-heavy senior) の 3 demand を計画し journal に decision-level synthesis パターンのみ追記。`docs/user-demand-report-2026-05-03.md` の生成は未完了 — round 6 候補として残す |
| 2026-05-02 | Artisan | LP Quickstart を `npx refscope` 形式に更新。Hero meta line を `One command` に変更。BeyondMvp に `Static export` を追加。lp-structure.md に Changelog section 追記 | `apps/web/src/components/Quickstart.astro`, `apps/web/src/components/Hero.astro`, `apps/web/src/components/BeyondMvp.astro`, `docs/brand/lp-structure.md` | build PASS (warning/error ゼロ)、avoid words 違反ゼロ |
| 2026-05-02 | Nexus | icon+text ボタンの 2 行レイアウト修正 — ユーザー画像で TopBar (Hide/Summary/Quiet/CVD/Pause) と DiffToolbar (Expand all/Collapse all/Full/Copy) で icon と text が縦積みになる問題を確認。原因は `.rs-compact-button` に `display` 指定がなく inline-block、Tailwind preflight の `svg { display: block }` で icon が改行を生成。CSS 1 箇所に `display: inline-flex; align-items: center;` を追加して 9 ボタンを一括 1 行化。併せて `CommitTimeline.tsx:828` の commit subject 行 `gap-y-1` → `gap-y-2` (4→8px) も維持 (ref badge 折り返し時の余白) | `mock/src/app/App.tsx`, `mock/src/app/components/refscope/CommitTimeline.tsx` | build PASS / 新規依存・新規 token ゼロ、`marginRight: 4` の icon inline style は維持 (副作用なし) |
| 2026-05-02 | Artisan | Vision audit P1: F-02 (skip link WCAG 2.4.1)、F-07 (z-index semantic tokens 5 種 + 3 箇所置換)。F-01 (--rs-border L=34→52%) はユーザーフィードバックで「前より見えにくい」と即時ロールバック — 49 箇所の panel divider が一斉に明るくなり視覚ノイズ増。WCAG 1.4.11 の数値達成と主観的可読性のトレードオフが顕在化。border を `--rs-border-control` (3:1 必要) と `--rs-border-subtle` (装飾) に分離する 2 段階 token 設計を follow-up として記録 | `mock/src/app/App.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx`, `mock/src/app/components/refscope/DiffViewer.tsx` | build PASS / API tests 85/85 PASS、新規依存ゼロ |
|---|---|---|---|---|
| 2026-05-01 | Artisan | Palette E-04/E-02/E-05 実装 (F7/F8/F9) | `mock/src/app/App.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommitTimeline.tsx`, `mock/src/app/hooks/useTimelinePrefs.ts` (新規) | F7: --rs-text-muted L=55%→62%、Prism comment L=60%→63%、CVD comment L=55%→62% (WCAG AA 達成)。F8: Author/Path 入力を `hidden xl:flex` でレスポンシブ化、⌘K バッジに title 追加。F9: `useTimelinePrefs` hook 新規作成 (rAF+localStorage+schema validation)、CompareBarCollapsible+ActivityGraphCollapsible wrapper で accordion 化、active 時/summaryView 時の強制展開ロジック実装。build PASS / API tests 85/85 PASS |
| 2026-05-01 | Echo + Artisan (via Nexus) | 「画面の見え方」改善 — Echo 4 ペルソナ walkthrough で 14 件抽出 → Top 3 + 一貫性 3 件を実装 | `mock/src/app/components/refscope/TopBar.tsx`, `BranchSidebar.tsx`, `CommitTimeline.tsx`, `PeriodSummaryView.tsx` | (F1) `PAUSED ${n}` を中黒区切り `PAUSED · ${n}` に変更し数値を視覚分離。(F2) `{isQuiet ? "Quiet" : "Quiet"}` のデッドコード修正 — Quiet/CVD/Summary 3 ボタンを ON 時 `Xxx on` 表示にして色非依存で状態弁別。(F3) TopBar 右側ボタン群を既存 `Separator` で 3 グループ (status / layout / view / live) に分割し機能境界を可視化。(F4) `CommitRow` に `tabIndex={0}` + Enter/Space onKeyDown と `group-focus-within:opacity-100` を追加し `↵ open` ヒントをキーボードフォーカス時にも表示。(F5) `--rs-accent` 上書きは副作用大と判断し却下、代わりに `RefMapBar` に branch=`█` / remote=`▒` / tag=`◆` の `aria-hidden` symbol を追加し CVD-safe 環境でも形状で判別可。(F6) `ObservedZone`/`DerivedZone` heading に `[OBS]`/`[DRV]` の monospace 枠付きバッジを追加し、警告色が縮退する CVD 環境でも区画境界を text で読み取り可能。検証: `pnpm build:mock` PASS / API tests 85/85 PASS、新規依存ゼロ、新規 CSS variable ゼロ、aria-* と data-quiet/data-color-vision stacking 規則維持 |
| 2026-04-30 | Plea | 合成ユーザー需要レポートを作成 | `docs/user-demand-report.md` | setup、信頼性、アクセシビリティ、team review に関する需要仮説を抽出 |
| 2026-04-30 | Builder | Mina/D1 の 1 コマンド起動診断を補強 | `scripts/dev/validate-repos.sh`, `Makefile`, `README.md` | `make dev-app` / `make dev-api` の前に repo path を検査し、未指定・相対 path・存在しない path・Git root ではない path を平易に説明 |
| 2026-04-30 | Nexus | timeline に activity overview を追加 | `mock/src/app/components/refscope/CommitTimeline.tsx` | commit 数、追加/削除、signed、merge、new をラベル付きメトリクスとミニバーで可視化 |
| 2026-04-30 | Nexus | sidebar に ref map を追加 | `mock/src/app/components/refscope/BranchSidebar.tsx` | branches、remotes、tags の分布と選択中 ref をラベル付き SVG/バーで可視化 |
| 2026-04-30 | Nexus | detail panel に change graph を追加 | `mock/src/app/components/refscope/DetailPanel.tsx` | 選択 commit の additions/deletions 比率とファイル別変更量をラベル付きバーで可視化 |
| 2026-04-30 | Nexus | compare mode に graph を追加 | `mock/src/app/components/refscope/CommitTimeline.tsx` | ahead/behind と added/deleted をラベル付きバーで可視化 |
| 2026-04-30 | Nexus | top bar に live pulse を追加 | `mock/src/app/components/refscope/TopBar.tsx` | realtime status と pause 中 pending updates を pulse bar で可視化 |
| 2026-04-30 | Nexus | rewrite alert に flow visual を追加 | `mock/src/app/components/refscope/BranchSidebar.tsx` | previous hash から current hash への rewrite flow を SVG とラベルで可視化 |
| 2026-04-30 | Nexus | activity overview に author graph を追加 | `mock/src/app/components/refscope/CommitTimeline.tsx` | 表示中 commits の author distribution をラベル付きバーで可視化 |
| 2026-04-30 | Nexus | detail panel に file status mix を追加 | `mock/src/app/components/refscope/DetailPanel.tsx` | changed files の status 構成を分割バーと凡例で可視化 |
| 2026-05-01 | Plea | 第 2 ラウンドの合成ユーザー需要を生成 | `docs/user-demand-report-2026-05-01.md`, `.agents/plea.md` | Hana (週次 / 非エンジニア)、Tomo (大規模 OSS)、Yuki (sensory-sensitive)、Ken (on-call) の 4 demand を抽出。死角は「開きっぱなし前提」「観測事実と派生の分離不徹底」「visual summary の抑制装置欠如」 |
| 2026-05-01 | Spark (via Nexus) | Hana demand に対する「期間サマリビュー」proposal | `docs/spark-period-summary-proposal.md` | 派生強度別 3 options (Observed-only / Rule-based 推奨 / LLM narrative opt-in)。Option B は新コマンド不要 (`log --since --until --name-status --numstat`) で `validation.js` 拡張のみ。RICE / Open questions 7 / Assumptions 6 含む。Next: Rank |
| 2026-05-01 | Rank (via Nexus) | round 2 demand 4 件の priority scoring | `docs/rank-round2-priority.md` | RICE / WSJF / MoSCoW / ICE 横断。推奨順序 Yuki (Must, 横串先行) → Hana (Should, Spark readiness) → Ken (Should + Spike, reflog allowlist 追加要 Atlas/Magi review) → Tomo (Could, MVP 範囲外 v2)。synthetic confidence cap 45-60%、Researcher 検証必須は Ken と Tomo |
| 2026-05-01 | Artisan (via Nexus) | D-Yuki Quiet mode 実装 | `mock/src/app/hooks/useQuietMode.ts` (新規), `mock/src/app/App.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx` | `data-quiet` root attribute + CSS で transition / animation を 0ms 化し OKLCH chroma を縮小 (lightness 維持で WCAG 2.2 AA contrast 確保)。manual pause (`livePaused`) と Quiet 由来 auto pause (`isQuiet`) を独立保持し合成、SSE event は両者で pending キューに流れ Quiet 解除時に polite live region で告知 (focus 移動なし)。`prefers-reduced-motion` 同期、localStorage 永続化、aria-pressed / aria-live / accessible name 完備。build と API tests 通過。ブラウザ目視は未実施 |
| 2026-05-01 | Spark (via Nexus) | Tomo demand に対する file-level history proposal (v2 候補 RFC) | `docs/spark-tomo-file-history-proposal.md` | 派生強度別 3 options (cursor のみ / `--follow` + literal rename evidence 推奨 / threshold + year-jump)。Option B 推奨: 新 endpoint `/files/history`、opaque base64 cursor (`{v:1, d, h}`)、`--date-order` で stable ordering。Rename は Git literal `R<NN>\told\tnew` を payload にそのまま転記し UI で "Git reported rename — similarity NN%" と併記、Refscope 側は判定しない。gitRunner allowlist 変更不要、200 commit/request hard limit 維持。Open questions 8 / Assumptions 7。Next: Researcher (大規模 monorepo maintainer 検証) |
| 2026-05-01 | Builder (via Nexus) | D-Hana 期間サマリビュー API 実装 | `apps/api/src/validation.js`, `apps/api/src/gitService.js`, `apps/api/src/http.js`, `apps/api/test/validation.test.js` (新規), `apps/api/test/gitService.summary.test.js` (新規), `apps/api/test/http.test.js` | `GET /api/repos/:repoId/commits/summary?since&until&groupBy&ref` を Spark Option B contract で追加。`parseDateQuery` / `parseGroupByQuery` を validation に追加、`summarizeCommits` で単一 `git log` 呼び出し (`--no-show-signature --no-renames --numstat --no-ext-diff --no-textconv --max-count=201 --since --until --end-of-options <hash> --`)。観察事実 (literal regex `/^([a-z][a-z0-9-]*)(?:\([^)]*\))?:\s/`) と派生 (uncategorized 隔離) の境界をコメント + ロジックで明示。tests 58 → 85 件、200 commit hard cap は `truncated: true` で透明化、gitRunner allowlist 変更なし |
| 2026-05-01 | Artisan (via Nexus) | D-Hana 期間サマリビュー UI 実装 | `mock/src/app/components/refscope/PeriodSummaryView.tsx` (新規, 19 KB), `mock/src/app/api.ts`, `mock/src/app/App.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx` | "Observed (no interpretation)" と "Derived (rule-based grouping, no AI)" を別カードで分離 (既存 rewrite alert の warning border 再利用)、period preset (today / this-week monday-start / last-30-days / custom YYYY-MM-DD) と groupBy tabs (prefix / path / author)、truncated バッジ "Showing first 200 commits"、uncategorized 別カードに "No conventional-commit prefix detected (kept separate, not guessed)"。drilldown は既存 timeline filter に流す最小実装 (prefix→search、path→path filter、author→author filter)。command palette に Toggle period summary、TopBar に summary toggle 追加。Quiet mode の `data-quiet` CSS と互換、新規依存ゼロ、新 CSS variable ゼロ |
| 2026-05-01 | Artisan (via Nexus) | diff viewer 本格改修 (旧 viewer は行番号偽装) | `mock/src/app/lib/parseUnifiedDiff.ts` (新規, 434 行), `mock/src/app/components/refscope/DiffViewer.tsx` (新規, 913 行), `mock/src/app/components/refscope/DetailPanel.tsx`, `mock/src/app/api.ts`, `mock/src/app/App.tsx` | unified diff を `ParsedDiff` / `DiffFile` / `DiffHunk` / `DiffLine` の AST にパース (throw しない、changeKind 8 種、similarity / isBinary / fallback 推定)、行番号は hunk header の `@@ -a,b +c,d @@` から正確に計算。Viewer 機能: file list + viewMode all/single + wordWrap + showWhitespace (タブ/空白を muted span でデコレート) + path filter + Expand/Collapse all + file 数 30 超で初期 collapse + 個別/全体コピー + truncated 警告。aria-pressed / aria-expanded / aria-selected / aria-label / aria-live 完備、role="grid" 採用、quiet mode 互換 (data-quiet CSS)。API 変更ゼロ、新規依存ゼロ、新 CSS variable ゼロ |
| 2026-05-01 | Artisan (via Nexus) | DiffViewer に fullscreen モード追加 | `mock/src/app/components/refscope/DiffViewer.tsx`, `mock/src/app/components/refscope/DetailPanel.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx`, `mock/src/app/App.tsx` | Controlled/uncontrolled hybrid (`fullscreenProp` + `onFullscreenChange`) で内部 toggle と CommandPalette からの操作を統合。CSS overlay (`role="dialog" aria-modal="true"` + `position: fixed; inset: 0; z-index: 50`)、body scroll lock + cleanup、Escape 復帰、focus return (元 toggle へ自動移動)、commit 切替で auto close。CommandPalette に "Toggle diff fullscreen" を追加 (diff 利用可能時のみ表示)。アイコンは lucide-react の Maximize2 / Minimize2、新規依存ゼロ、新 CSS variable ゼロ、API 変更ゼロ |
| 2026-05-01 | Artisan (via Nexus) | 3 ペインを resizable + sidebar 折りたたみ + デフォルト比率調整 (Diff 領域 ~25% → ~40%) | `mock/src/app/hooks/useLayoutPrefs.ts` (新規, 166 行), `mock/src/app/App.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx`, `mock/src/app/components/refscope/BranchSidebar.tsx`, `mock/src/app/components/refscope/DetailPanel.tsx` | shadcn `Resizable` primitive (`react-resizable-panels` wrap) で 3 ペインを drag handle 付きリサイズ可能に。デフォルト比率 sidebar 18 / center 42 / detail 40、min/max (sidebar 12-30、center 25+、detail 25-70)。Sidebar collapse は `ImperativePanelHandle.collapse/expand()` 経由、`onLayout` rAF coalesce で localStorage `refscope.layoutPrefs.v1` に永続化 (schema validation + 破損時 default fallback)。TopBar に `PanelLeftClose/Open` トグル (aria-pressed)、CommandPalette に "Toggle branch sidebar" コマンド。BranchSidebar/DetailPanel の固定幅 (248px/460px) を撤廃して panel が幅を所有。新規依存ゼロ、新 CSS variable ゼロ、API 変更ゼロ |
| 2026-05-01 | Nexus | overflow 修正 (commit row の長い ref badge / DiffViewer の hunk header 縦割れ + 行末切れ) | `mock/src/app/components/refscope/CommitTimeline.tsx`, `mock/src/app/components/refscope/DiffViewer.tsx` | (1) commit subject 行に `flex-wrap` + `gap-x/y` を追加し、`HEAD -> feat/...` 等の長い ref badge が枠外に出ず次行へ折り返すよう変更。(2) DiffViewer の hunk row を `gridTemplateColumns: 1fr` から `auto` + `minWidth: 100%` に変更し、`@@ -3,8 +3,6 @@` ヘッダ span に `whiteSpace: nowrap` を付与して縦割れを排除。(3) DiffLineRow も wordWrap オフ時は `auto` + `minWidth: 100%` + content `flexShrink: 0` に切替え、長い行が parent の `overflow-x-auto` で水平スクロールされるよう修正 (wordWrap オン時は従来の `1fr` + `pre-wrap` を維持)。新規依存ゼロ、API 変更ゼロ |
| 2026-05-01 | Artisan | Lin (color-blind / deuteranomaly) demand 対応 — CVD-safe theme + 非色 diff signal | `mock/src/app/hooks/useColorVisionTheme.ts` (新規), `mock/src/app/App.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx`, `mock/src/app/components/refscope/DiffViewer.tsx` | `data-color-vision="default\|cvd-safe"` root attribute (data-quiet と独立)。Wong (2011) palette で add=orange oklch(74% 0.17 55) / del=blue oklch(50% 0.17 240)。非色 signal: `.rs-diff-add/.rs-diff-del/.rs-diff-context` クラスを DiffLineRow に常時付与、CVD-safe 時に left bar 4px + del 行に斜線 background-image。Prism token を keyword=vermilion / string=sky-blue / number=amber / function=blueish-green に差替え。TopBar "CVD" ボタン + CommandPalette "Toggle CVD-safe theme"。localStorage 永続化。quiet+CVD 重ね時は lightness を保ったまま chroma を縮小。build 通過、API tests 85/85 通過 |
| 2026-05-01 | Plea | 第 3 ラウンドの合成ユーザー需要を生成 | `docs/user-demand-report-2026-05-01-r3.md`, `.agents/plea.md` | Eri (PM / Slack URL 受信)、Lin (color-blind frontend)、Riku (CLI fluent SRE / pickaxe)、Asha (multi-client contractor / screen share) の 4 demand を抽出。最大の死角は「Refscope を自分のマシンの画面の中で完結するツールと前提しているが、screen share / 共有 URL / screenshot で境界の外側に頻繁に出る」。journal に「境界外側ユース」と「visualization 軸ごとの accessibility demand 連鎖」の 2 パターンを追記 |
| 2026-05-01 | Spark (via Nexus) | Riku demand に対する Advanced search modes proposal | `docs/spark-riku-search-modes-proposal.md` | 派生強度別 3 options (A: mode chip + segmented control 推奨 / B: DSL prefix `S:`/`G:`/`g:` / C: hybrid)。API 設計は既存 `/commits` の query parameter 拡張 (`?mode=pickaxe&pattern=...`) を推奨、`search` との同時指定は 400。`-S` は attached form (`-STOKEN`) で hyphen-leading pattern (`-delete` 等) の誤解釈を回避、`--end-of-options` 規律維持。**gitRunner allowlist 変更不要** (実コード確認: `-S`/`-G`/`--grep` は dangerous flag rejection list 対象外、Atlas/Magi review skip)。validation 拡張 `parseSearchModeQuery` / `parsePickaxeQuery` / `parseRegexQuery` (max 200 chars、control char 拒否、ReDoS 評価は Git 側に委ねる)。pickaxe match 行は commit row inline 表示せず diff viewer での pattern highlight に限定 (観測 vs 派生境界)。RICE 3.0 (Quick Win)、Open questions 8 / Assumptions 7。Next: Researcher (実 power user 検証) / Rank (priority scoring) / Builder (PROTOTYPE) |
| 2026-05-02 | Builder | Hunk timeline 実装 — DetailPanel から file 単位の commit 履歴を hunk 積層表示 | `apps/api/src/gitService.js`, `apps/api/src/http.js`, `apps/api/test/fileHistory.test.js` (新規), `mock/src/app/api.ts`, `mock/src/app/components/refscope/FileHistoryView.tsx` (新規), `mock/src/app/components/refscope/DetailPanel.tsx`, `mock/src/app/App.tsx` | `GET /api/repos/:repoId/files/history?path&ref&limit` を Magi 採択 contract で追加。`getFileHistory` は `--follow --find-renames --patch --no-show-signature --no-ext-diff --no-textconv --end-of-options <hash> -- <literal-pathspec>` の単一 `git log` 呼び出し、`--max-count=limit+1` で truncation 検知。raw patch は payload に**そのまま**転記 (UI 側で既存 `parseUnifiedDiff` AST 化)、rename は Git literal `R<NN>` を payload に保持し UI は "Git reported rename — similarity NN%" と併記 — Refscope 側は判定しない。validation は既存 `parsePathQuery` / `parseLimitQuery` / `isValidGitRef` 再利用、empty path → 400 "Missing path parameter"。default limit 20 / max 50。FileHistoryView は modal-ish overlay (role="dialog" aria-modal、Esc 閉、focus restore、body scroll lock)、CVD-safe theme は `.rs-diff-*` 経由で自動継承、Quiet mode は root attribute なので無改修で互換。CommandPalette command は scope (オプション) のため state lift 回避で見送り。tests 85 → 119 件 (+8 file-history tests + sub-tests)、build pass、gitRunner allowlist 変更なし、新規依存ゼロ |
| 2026-05-02 | Builder | Branch Drift Halo 実装 — BranchSidebar の各 ref に ahead/behind を halo+aria で常時表示、SSE 連動 | `apps/api/src/gitService.js`, `apps/api/src/http.js`, `apps/api/test/refsDrift.test.js` (新規), `mock/src/app/api.ts`, `mock/src/app/components/refscope/BranchSidebar.tsx`, `mock/src/app/App.tsx` | `GET /api/repos/:repoId/refs/drift?base&limit` を Magi 採択 ★2 contract で追加。`getRefDrift` は `for-each-ref` で全 ref を取得 → branch + remote のみ filter (tag は drift の意味薄、wire layer で除外) → limit (default 50 / max 100) で打ち切り `truncated:true`、各 ref に対し `compareRevListArgs(refHash, baseHash)` / `compareRevListArgs(baseHash, refHash)` / `compareMergeBaseArgs` を `Promise.all` 並列実行 (3N git calls)、ref hash === base hash で短絡。base/target は別トークン維持 (triple-dot 不使用)、既存 helper (`compareRevListArgs` / `compareMergeBaseArgs` / `resolveCommitishRevision` / `parseLimitQuery` / `isValidGitRef`) のみ再利用、gitRunner allowlist 変更ゼロ。UI: `BranchSidebar` に `driftMap` prop を追加、各 branch / remote 行右端に `<DriftHalo>` SVG (18px×12px、ahead=accent / behind=text-muted、`aria-label="N ahead, M behind of HEAD"` + tooltip)、bar 長さは可視 ref の max(ahead+behind) で正規化 (派生はこの 1 点のみ)、tag 行は元から halo 対象外。`App.tsx` は `driftMap` state + `fetchRefDrift` (AbortController で前 in-flight 取消) を保持、SSE `commit_added` / `ref_*` / `history_rewritten` で 500ms debounce refetch を schedule。base は MVP では HEAD 固定 (将来切替 UI 用に payload は `base.input/resolved` を露出)。tests 119 → 126 件 (+7 refsDrift tests)、build pass、make verify 8/8 PASS、新規依存ゼロ、新規 CSS 変数ゼロ、Quiet / CVD-safe theme は既存 token 経由で自動互換 |
| 2026-05-02 | Builder | Working tree changes view 実装 — uncommitted (staged/unstaged) を CommitTimeline 最上段の pseudo-row + DetailPanel タブで表示、tracked のみ MVP | `apps/api/src/gitService.js`, `apps/api/src/http.js`, `apps/api/test/workTree.test.js` (新規), `mock/src/app/api.ts`, `mock/src/app/App.tsx`, `mock/src/app/components/refscope/CommitTimeline.tsx`, `mock/src/app/components/refscope/DetailPanel.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx` | `GET /api/repos/:repoId/worktree` を追加。`getWorkTreeChanges` は 4 並列 `runGit` (`diff --cached --numstat` / `diff --cached --patch` / `diff --numstat` / `diff --patch`)、`--end-of-options` / `--no-color --no-ext-diff --no-textconv` / `--find-renames` (patch のみ) 規律。numstat は既存 `parseNumstatSummary` 再利用、patch text はそのまま raw 文字列として payload に転記 (UI 側で既存 `parseUnifiedDiff` AST 化)。`notes.untrackedExcluded: true` を必ず付与 — `status` / `ls-files` は allowlist 外なので tracked のみ MVP の境界を payload + UI 双方で明示。allowlist 変更ゼロ。UI: `WorkTreeRow` を CommitTimeline 最上段 pseudo-row として実装 (fileCount > 0 時のみ表示)、dashed top-border + "Not yet committed" / "DRV" バッジで commit row と視覚差別化、Enter/Space + aria-pressed で keyboard accessible、refresh icon は stopPropagation で row 選択と分離。`WorkTreePanel` を DetailPanel に統合、Staged/Unstaged の手書きタブ (Radix Tabs を回避し既存 token と整合)、empty state は side ごとに表示、片方のみ変更ある場合は自動で activeTab を flip、フッタに "Untracked files are not shown" 注釈。`selectedWorkTree: boolean` 独立 state を導入し既存 `selected` (commit hash) と相互排他 (`handleSelectCommit` / `handleSelectWorkTree` でクリア)、repo 切替でも reset。`fetchWorkTree` は AbortController + 500ms debounce、window focus event + 手動 refresh で auto reload。TopBar に "Worktree" refresh button (lucide RefreshCw)、CommandPalette に "Refresh working tree" (常時) + "Show working tree changes" (workTreeAvailable 時) を追加。Quiet mode は root attribute、CVD-safe theme は既存 var(--rs-*) 経由で無改修互換。tests 126 → 134 件 (+8 worktree tests: clean / unstaged-only / staged-only / both / rename / untracked excluded / numstat-args / patch-args)、build pass (`pnpm build:api` `pnpm build:mock`)、make verify 8/8 PASS、新規依存ゼロ、新規 CSS 変数ゼロ |
| 2026-05-02 | Builder | File history を top-level で開く動線追加 — CommandPalette + TopBar + path 入力ダイアログ + autocomplete | `mock/src/app/components/refscope/FileHistoryPrompt.tsx` (新規), `mock/src/app/App.tsx`, `mock/src/app/components/refscope/DetailPanel.tsx`, `mock/src/app/components/refscope/TopBar.tsx`, `mock/src/app/components/refscope/CommandPalette.tsx` | FileHistoryView の表示制御を DetailPanel から App.tsx に lift (`fileHistoryPath` / `fileHistoryPromptOpen` state)、3 入口 (CommandPalette "Open file history…" / TopBar の History アイコンボタン / DetailPanel 既存 history ボタン) が同じ overlay を駆動。新規 `FileHistoryPrompt` は role="dialog" aria-modal、focus trap+restore、Escape で close、`validatePath` (既存 `parsePathQuery` と同 semantics: 200 char、control char、leading `/`/`-`、`.`/`..`/empty segment 拒否) でリアルタイム検証、autocomplete 候補を keyboard-driven listbox (max 8) で表示、`localStorage` "refscope.fileHistory.recentPaths.v1" (max 10、schema validation 込み) + selectedCommit.files を merge して dedupe。DetailPanel の React Hooks rule 違反 (早期 return 後の `useState`) も lift と同時に修正。後方互換 — DetailPanel の changed-files row history ボタンは onClick だけ App.state 書き換えに変更し動作据え置き、`FileHistoryView` の props は無変更。Quiet / CVD-safe theme は既存 token 経由で自動互換。API 変更ゼロ、tests 134/134 PASS、`pnpm build:mock` PASS、make verify 8/8 PASS、gitRunner allowlist 変更ゼロ、新規依存ゼロ、新規 CSS 変数ゼロ |
| 2026-05-02 | Builder | Related files (co-change) 実装 — File History サイドバー下部に頻繁共編集ファイル top-K + クリックで遷移 | `apps/api/src/gitService.js`, `apps/api/src/http.js`, `apps/api/test/relatedFiles.test.js` (新規), `mock/src/app/api.ts`, `mock/src/app/components/refscope/FileHistoryView.tsx`, `mock/src/app/App.tsx` | `GET /api/repos/:repoId/files/related?path&ref&limit` を追加。`getRelatedFiles` は 2-step git log で N+1 を回避: (1) `--follow --format=%H` で対象 path を触った直近 commit hashes を取得 (`limit+1` 読み truncated 検知、default 30 / max 50)、(2) `--no-walk --name-only --format=<sep>%H%x00%aI ... <hash1>...<hashN>` で N コミットの changed-paths を 1 syscall に集約。集計は target 除外 + 同一 commit 内 dedupe、`normalizeNumstatPath` で `path/{old=>new}` 正規化、count desc → lastCoChangeAt desc → path asc で stable sort、top 20 (`RELATED_FILES_TOP_K`) を返却。base は `resolveCommitishRevision` で hash 化、`--end-of-options` / `formatLiteralPathspec` 規律、新規 helper (`relatedFilesHashLogArgs` / `relatedFilesNameLogArgs` / `parseRelatedFilesRecords` / `readRelatedFilesQuery`) を export して unit test 可能に。observation 事実 (commit 群の name list + authorDate) と派生 (count + lastCoChangeAt + sort + top-K slice) の境界はコメントで明示、similarity / coupling score 等は推測しない。UI: `FileHistoryView` に `onSwitchFile` prop を追加 (React 予約名衝突回避のため非 `ref\* 命名)、aside 内 CommitList の下に `RelatedFilesPanel` を配置。collapsible (ChevronDown/Right + Network icon)、各エントリ = path (truncate, mono) + co-change count バッジ (× N、accent bg) + 相対時間。最大 10 件、超過分は "+N more" で展開、`data.truncated` 時は "Scanned the M most-recent commits…" を併記。クリックで `onSwitchFile(path)` → 既存 `submitFileHistoryPath` ハンドラを再利用 → recent-paths 永続化 + overlay の `filePath` 切替で grpah/list が新 target で再 fetch。AbortController で前 in-flight 取消、target 切替時に "show all" toggle reset、loading / error / empty states 完備。Quiet mode + CVD-safe theme は既存 token 継承で無改修互換。tests 134 → 148 件 (+14 relatedFiles tests: hash-only args / no-walk name-only args / parser aggregation+dedupe+top-K / 実 repo basic / rename via --follow / lonely target empty / truncated / branch ref isolation / 全 query 不正系 8 ケース / 404 unknown ref / max limit clamp / 定数 sanity)、`pnpm build:api` (`node --check`) PASS、`pnpm build:mock` PASS、make verify 8/8 PASS、gitRunner allowlist 変更ゼロ、新規依存ゼロ、新規 CSS 変数ゼロ |
